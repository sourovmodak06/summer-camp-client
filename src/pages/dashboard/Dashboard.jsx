import useTitle from "../../hooks/useTitle";


const Dashboard = () => {
    useTitle("Dashboard");
    return (
        <div>
            <h2>Dashboard Page</h2>
        </div>
    );
};

export default Dashboard;